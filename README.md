This branch is for Style Editor integration in the browser.
(built-in version, not an add-on)

The feature is disabled by default.
To enable pref 'devtools.style_editor.enabled' must be set to 'true'.
